import './PersonTwoMessage.css'

const PersonTwoMessage = ({message}) => {

    return (
        <section id="player-two-message">
            <p>{message}</p>
        </section>

    )

}
export default PersonTwoMessage